1、系统启动时，u-boot从QSPI flash读取kernel文件的速度优化：
  在<project-path>/project-spec/meta-plnx-generated/recipes-bsp/u-boot/configs/platform-auto.h中，在cp_kernel2ram中添加读取速度：
  cp_kernel2ram=sf probe 0 <speed_hz> &…
  注：
    i. speed_hz值为Hz计算，宜根据QSPI flash相关参数选择合适的数值，过小读取速度太慢，过大则会造成读取的数据出错；
    ii. 此优化可能会在执行petalinux-config后被修改，如想一劳永逸，请修改petalinux安装目录下的u-boot_bsp.tcl文件
     （文件路径文件路径<petalinux-path>/etc/hsm/scripts/libs/）第483行左右：
      ...
      "kernel" {
          append data "\n" { "cp_kernel2ram=sf probe 0 <speed_hz> && sf read ${netstart} ${kernelstart} ${kernelsize}\0" \ }
      }
      ...
      但此文件修改后，每次在项目中执行petalinux-config后，将会对项目中原有的platform-auto.h文件进行修改,
